import 'package:flutter/material.dart';

class SellReceiptsScreen extends StatelessWidget {
  const SellReceiptsScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(
        child: Text('Sell Screen'),
      ),
    );
  }
}
